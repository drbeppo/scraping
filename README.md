# ウェブクローラー（スクレイピング）をPythonで作ってみました。
# クローラーを作る場合、絶対にサーバーに過度に負荷をかけてはいけません。
# クローリング先のURLは隠しています。

# I make a web crawer(scraping) using Python.
# NOTICE: Do not put a overload on the server.
# I hide the target url.
